// Common
app.controller('cribsController', ['$scope', '$http', 'dataService', function($scope, $http, dataService) {

    // $scope.cribs;

    // $scope.cribs = dataService.getData();

    // var promise = dataService.getData();
    //
    // promise.then(function(data){
    //   $scope.cribs = data.data;
    // });

}]);
